@startuml
!theme plain
autonumber
skinparam maxMessageSize 150
skinparam ParticipantPadding 20
skinparam BoxPadding 10

title Sơ đồ tuần tự (Sequence Diagram) - Luồng Đăng ký tài khoản (Register)

actor "Người dùng" as Guest
participant "Giao diện (UI)" as UI
participant "AuthController" as Controller
participant "AuthFacade" as Facade
participant "RedisService" as Redis
participant "MailService" as Mail
participant "UserDAO" as DAO
database "PostgreSQL\n(Database)" as DB

Guest -> UI : Truy cập trang Đăng ký\nNhập thông tin (Username, Email, Pass...)
activate UI

UI -> Controller : POST /register
activate Controller

Controller -> Redis : checkSpam(ipAddress)
activate Redis
Redis --> Controller : Kết quả kiểm tra
deactivate Redis

alt Bị chặn do Spam
    Controller --> UI : HTTP 429 Too Many Requests
    UI --> Guest : Báo lỗi "Vui lòng đợi"
else Hợp lệ
    Controller -> Facade : requestRegister(RegisterRequest)
    activate Facade

    Facade -> DAO : existsByUsernameOrEmail(username, email)
    activate DAO
    DAO -> DB : SELECT EXISTS(...)
    activate DB
    DB --> DAO : true/false
    deactivate DB
    DAO --> Facade : Kết quả trùng lặp
    deactivate DAO

    alt Trùng Email hoặc Username
        Facade --> Controller : Ném ngoại lệ (Tài khoản đã tồn tại)
        Controller --> UI : Báo lỗi trùng thông tin
        UI --> Guest : Hiển thị lỗi trên form
    else Thông tin hợp lệ
        Facade -> Facade : BCrypt.hashpw(password)
        Facade -> DAO : save(User, status=PENDING)
        activate DAO
        DAO -> DB : INSERT INTO users
        activate DB
        DB --> DAO : Lệnh thành công
        deactivate DB
        DAO --> Facade : Trả về User đã lưu
        deactivate DAO

        Facade -> Redis : generateAndStoreOTP(email)
        activate Redis
        Redis --> Facade : Trả về OTP
        deactivate Redis

        Facade -> Mail : sendActivationEmail(email, OTP)
        activate Mail
        Mail --> Facade : Kết quả gửi Mail
        deactivate Mail

        Facade --> Controller : Trả về kết quả Thành công
        Controller --> UI : HTTP 302 Redirect (/verify)
        UI --> Guest : Chuyển sang trang nhập OTP
    end
    deactivate Facade
end

deactivate Controller
deactivate UI

@enduml
